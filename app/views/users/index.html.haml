.row
  .col-md-3
  .col-md-6
    .center
      %h1 List of all users
      You can see favorite photos of each user (just select user e-mail)
      %hr/

.center
  - @users.each do |user|
    %p= link_to user.email, user_path(user.id)

= render "shared/navigation"
